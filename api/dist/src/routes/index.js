"use strict";
/* import { Router } from "express";
const router = Router();

//espacio para configurar las rutas

router.get("/", (req, res) => {
  res.send("esta es la ruta get ");
});

export default router; */
//# sourceMappingURL=index.js.map